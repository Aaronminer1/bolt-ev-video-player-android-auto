<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PROJECTION" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="androidx.car.app.ACCESS_SURFACE" />
    <uses-permission android:name="androidx.car.app.NAVIGATION_TEMPLATES" />

    <application
        android:name=".BoltApp"
        android:allowBackup="true"
        android:label="@string/app_name"
        android:icon="@drawable/ic_launcher"
        android:roundIcon="@drawable/ic_launcher"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.DeviceDefault.Light">

        <!-- Phone-side launcher activity to request storage permission -->
        <activity
            android:name=".PermissionActivity"
            android:exported="true"
            android:theme="@android:style/Theme.DeviceDefault.Light">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Car App Service -->
        <service
            android:name=".BoltPlayerCarAppService"
            android:label="@string/app_name"
            android:exported="true">
            <intent-filter>
                <action android:name="androidx.car.app.CarAppService" />
                <category android:name="androidx.car.app.category.NAVIGATION" />
            </intent-filter>
        </service>

        <meta-data
            android:name="androidx.car.app.minCarApiLevel"
            android:value="2" />

        <activity
            android:name=".CrashActivity"
            android:exported="false"
            android:theme="@android:style/Theme.DeviceDefault.Light" />

        <activity
            android:name=".GoogleSignInActivity"
            android:exported="false"
            android:label="YouTube Sign In"
            android:theme="@android:style/Theme.DeviceDefault.Light" />

        <activity
            android:name=".PhoneYouTubeActivity"
            android:exported="false"
            android:launchMode="singleTop"
            android:label="YouTube"
            android:theme="@android:style/Theme.DeviceDefault.Light" />

        <!-- Screen mirroring: phone-side permission dialog -->
        <activity
            android:name=".MirrorActivity"
            android:exported="false"
            android:theme="@android:style/Theme.Translucent.NoTitleBar" />

        <!-- Screen mirroring: foreground service holding MediaProjection (required Android 14+) -->
        <service
            android:name=".ScreenMirrorService"
            android:exported="false"
            android:foregroundServiceType="mediaProjection" />

        <meta-data
            android:name="com.google.android.gms.car.application"
            android:resource="@xml/automotive_app_desc" />

    </application>

</manifest>
